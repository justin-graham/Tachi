{
  "name": "@tachi/contracts",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "deploy:testnet": "forge script script/Deploy.s.sol --rpc-url base_sepolia --broadcast --verify",
    "deploy:mainnet": "forge script script/Deploy.s.sol --rpc-url base --broadcast --verify"
  }
}
