{
  "name": "@tachi/gateway-cloudflare",
  "version": "0.1.0",
  "description": "Cloudflare Workers wrapper for tachi gateway",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.24.4"
  },
  "dependencies": {
    "@tachi/gateway-core": "workspace:*"
  }
}
